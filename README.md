# Web preview

Static web/PWA preview of the iPad learning activities.

Run locally:

```bash
cd web
python3 -m http.server 4173
```

Then open `http://localhost:4173`.

This is not the native SwiftUI app. It is a browser version for quick iPad testing in Safari without Apple signing or Xcode deployment.
